import '../../../../css/page-frame/footer.scss';
import Logo from '../../logo-components/logo';

export default function PageFooter(props){
    return (
        <div className={"footer-root"}>
            <Logo logoStyle={"light"} />
        </div>
    )
}